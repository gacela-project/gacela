# Upgrading

Moved to [`UPGRADE.md`](../UPGRADE.md) in the repository root, which is where
Composer packages conventionally put it and where `gacela-project/container`
already keeps its own.

This file is a pointer so existing links keep working.
