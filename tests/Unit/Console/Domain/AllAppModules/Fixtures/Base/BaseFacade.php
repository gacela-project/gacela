<?php

declare(strict_types=1);

namespace GacelaTest\Unit\Console\Domain\AllAppModules\Fixtures\Base;

/**
 * An abstract class named like a module's Facade -- the shape a project's own
 * base facade takes. Abstract classes are not modules, and reporting one would
 * report every project that has a base facade.
 */
abstract class BaseFacade
{
}
