<?php

declare(strict_types=1);

namespace GacelaTest\Unit\Console\Domain\AllAppModules\Fixtures\Shop;

/**
 * Named exactly the way the scaffolder names a module's Facade, and missing the
 * one thing that makes it one. That is the whole fixture: `UndiscoveredFacade`
 * reports this shape, and a class that really extended `AbstractFacade` would
 * be a new module in `tests/` -- module counts are asserted elsewhere.
 */
class ShopFacade
{
}
